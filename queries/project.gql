query($slug: String $id: String) {
 project(slug: $slug id: $id) {
   id
   category {
     id
     name
     slug
      icon
   }
   tags {
     id
     name
     slug
   }
   name
   slug
   photos
   media
   description
 }  
}